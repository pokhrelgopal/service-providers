/** Kinds of verification media a provider uploads. */
export enum DocumentType {
  /** A live photo of the provider's face. */
  SELFIE = 'selfie',
  /** Citizenship / government ID image. */
  ID_DOCUMENT = 'id_document',
}
