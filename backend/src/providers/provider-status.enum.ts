/** Provider verification lifecycle: draft → submitted → approved / rejected. */
export enum ProviderStatus {
  DRAFT = 'draft',
  SUBMITTED = 'submitted',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}
